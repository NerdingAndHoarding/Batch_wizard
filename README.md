# Batch_wizard
Quick and easy way to create an LPC voice for Talkie Arduino Library (going-digital / Talkie).  Builds a graphic user interface program that can batch-process wave files into a vocab library and arduino test scetch.  Uses the python_wizard (ptwz python_wizard)
